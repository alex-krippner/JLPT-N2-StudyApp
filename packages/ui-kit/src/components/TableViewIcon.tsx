import React from "react";
import MuiTableViewIcon from "@mui/icons-material/TableView";
import { SvgIconProps } from "@mui/material";

export function TableViewIcon(props: SvgIconProps) {
  return <MuiTableViewIcon {...props} />;
}

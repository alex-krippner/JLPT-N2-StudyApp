import React from "react";
import MuiCancelIcon from "@mui/icons-material/Cancel";
import { SvgIconProps } from "@mui/material";

export function CancelIcon(props: SvgIconProps) {
  return <MuiCancelIcon {...props} />;
}

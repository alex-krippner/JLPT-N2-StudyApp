import React from "react";
import MuiAddIcon from "@mui/icons-material/Add";
import { SvgIconProps } from "@mui/material";

export function AddIcon(props: SvgIconProps) {
  return <MuiAddIcon {...props} />;
}
